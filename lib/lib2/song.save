## a song, 
## <name>
class Song

## that the <song> is readable/writablut by the object
attr_accessor = :neme, 

# class variable that stores all info into saved instances if  a 
@@all = []
  
  ## (that) accept <name> upon initialization.
  def inititlaze(:name)
   @name = name
  end


  def self.all
   @@all
  end
  
  def save
    @@all << self
  end
  
  def destroy_all 
    all.clear
  end

 def self.create
   Song.new
   Song.save
